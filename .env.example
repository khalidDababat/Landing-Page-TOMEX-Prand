# Public site metadata
VITE_APP_NAME=TOMEX

# json-server API that serves src/data/db.json (npm run server)
VITE_API_URL=http://localhost:3001

# Endpoint used by the contact form service (optional).
# When empty, the contact form resolves locally without a network request.
VITE_CONTACT_API_URL=
